name: Test React App
on: push
jobs:
  Test:
    runs-on: ubuntu-latest
    steps:
      - name: Get repository code
        uses: actions/checkout@v4
      - name: Install node modules
        run: npm ci
      - name: Test app
        run: npm run test